<!--
todo: Separar o back do front.

todo: Sucrase:
    ? Permite o uso de imports/export dos módulos do ES6

todo: Sequelize:
    ? Controla a base de dados.
        * Tudo q tiver dentro de migrations é relacionado a alterações no banco de dados.

todo: Regra de rotas:
    ? index -> Lista todos os usuários -> GET
    ? store/create -> Cria um novo usuário -> POST
    ? delete -> Apaga um usuário -> Delete
    ? show -> mostra um usuário -> GET
    ? update -> Atualiza um usuário -> PATCH ou PUT
 -->
